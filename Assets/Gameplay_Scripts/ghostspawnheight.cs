using UnityEngine;

public class ghostspawnheight : MonoBehaviour
{
    public float spawnYOffset = 4.13f;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
